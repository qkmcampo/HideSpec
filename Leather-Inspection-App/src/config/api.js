const PI_IP_ADDRESS = '192.168.100.112';
const PI_PORT = 5000;

export const API_BASE_URL = `http://${PI_IP_ADDRESS}:${PI_PORT}`;
export const WS_URL = `http://${PI_IP_ADDRESS}:${PI_PORT}`;

export default {
  API_BASE_URL,
  WS_URL,
  PI_IP_ADDRESS,
  PI_PORT,
};